node_modules/
.next/
dist/
target/
*.pyc
__pycache__/
.env
.env.local
.firebase-service-account.json
firebase-service-account.json
*.db
*.sqlite
.DS_Store
.claude/plans/
mineru-venv/
output/
PDF/
.claude/worktrees/
test-venv/
test-outputs/
scripts/.cache/

# Setup script artifacts (created by scripts/setup.sh + scripts/run.sh)
setup.log
.setup-*.log
.run-*.log
src-tauri/binaries/
src-tauri/resources/node-standalone/
src-tauri/resources/node-runtime/
src-tauri/resources/models/
app/.claude/
app/.cache/

# Internal pre-strip notes (CLAUDE.md, DESIGN.md, TODOS.md). Kept locally
# for reference; not tracked because they describe a closed-source state
# (activation keys, credit ledger, white-label rules) that no longer
# matches the public AGPL release. Public docs live at the repo root and
# in docs/ (not docs/internal/).
docs/internal/
